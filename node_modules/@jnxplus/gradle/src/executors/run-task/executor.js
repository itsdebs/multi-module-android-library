"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const tslib_1 = require("tslib");
const devkit_1 = require("@nx/devkit");
const _1 = require("../../../.");
const common_1 = require("@jnxplus/common");
function runExecutor(options, context) {
    return tslib_1.__awaiter(this, void 0, void 0, function* () {
        devkit_1.logger.info(`Executor ran for Run Task: ${JSON.stringify(options)}`);
        let projectPath = '';
        if (options.projectPath) {
            projectPath = options.projectPath;
        }
        else {
            projectPath = (0, _1.getProjectPath)(context);
        }
        const command = `${(0, _1.getExecutable)()} ${projectPath}:${options.task}`;
        const result = (0, common_1.runCommand)(command);
        if (!result.success) {
            return { success: false };
        }
        if (options.keepItRunning) {
            yield (0, common_1.waitForever)();
        }
        return { success: true };
    });
}
exports.default = runExecutor;
//# sourceMappingURL=executor.js.map