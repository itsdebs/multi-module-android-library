import { ExecutorContext } from '@nx/devkit';
import { RunTaskExecutorSchema } from './schema';
export default function runExecutor(options: RunTaskExecutorSchema, context: ExecutorContext): Promise<{
    success: boolean;
}>;
