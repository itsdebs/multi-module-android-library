import { ExecutorContext } from '@nx/devkit';
import { BootBuildImageExecutorSchema } from './schema';
export default function runBootBuildImageExecutor(options: BootBuildImageExecutorSchema, context: ExecutorContext): Promise<{
    success: boolean;
}>;
