import { ExecutorContext } from '@nx/devkit';
import { LintExecutorSchema } from './schema';
export default function runExecutor(options: LintExecutorSchema, context: ExecutorContext): Promise<{
    success: boolean;
}>;
