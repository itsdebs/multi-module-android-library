"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const tslib_1 = require("tslib");
const common_1 = require("@jnxplus/common");
const devkit_1 = require("@nx/devkit");
const _1 = require("../../../.");
function runExecutor(options, context) {
    return tslib_1.__awaiter(this, void 0, void 0, function* () {
        devkit_1.logger.info(`Executor ran for serve: ${JSON.stringify(options)}`);
        let projectPath = '';
        if (options.projectPath) {
            projectPath = options.projectPath;
        }
        else {
            projectPath = (0, _1.getProjectPath)(context);
        }
        let command = `${(0, _1.getExecutable)()} ${projectPath}:`;
        if ((0, common_1.getPluginName)(context) === '@jnxplus/nx-boot-gradle' ||
            options.framework === 'spring-boot') {
            command += 'bootRun';
            if (options.args) {
                command += ` --args='${options.args}'`;
            }
        }
        if ((0, common_1.getPluginName)(context) === '@jnxplus/nx-quarkus-gradle' ||
            options.framework === 'quarkus') {
            command += 'quarkusDev';
        }
        if ((0, common_1.getPluginName)(context) === '@jnxplus/nx-micronaut-gradle' ||
            options.framework === 'micronaut') {
            command += 'run';
        }
        const result = (0, common_1.runCommand)(command);
        if (!result.success) {
            return { success: false };
        }
        yield (0, common_1.waitForever)();
        return { success: true };
    });
}
exports.default = runExecutor;
//# sourceMappingURL=executor.js.map