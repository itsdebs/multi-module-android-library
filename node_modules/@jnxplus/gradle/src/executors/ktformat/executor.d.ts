import { KotlinFormatExecutorSchema } from '@jnxplus/common';
import { ExecutorContext } from '@nx/devkit';
export default function runExecutor(options: KotlinFormatExecutorSchema, context: ExecutorContext): Promise<{
    success: boolean;
}>;
