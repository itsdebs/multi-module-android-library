import { ExecutorContext } from '@nx/devkit';
import { SetVersionExecutorSchema } from './schema';
export default function runExecutor(options: SetVersionExecutorSchema, context: ExecutorContext): Promise<{
    success: boolean;
}>;
export declare function setVersion(buildGradleContent: string, newVersion: string): string;
