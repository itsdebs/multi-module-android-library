import { ExecutorContext } from '@nx/devkit';
import { BuildImageExecutorSchema } from './schema';
export default function runExecutor(options: BuildImageExecutorSchema, context: ExecutorContext): Promise<{
    success: boolean;
}>;
