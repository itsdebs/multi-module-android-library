import { ExecutorContext } from '@nx/devkit';
import { IntegrationTestExecutorSchema } from './schema';
export default function runExecutor(options: IntegrationTestExecutorSchema, context: ExecutorContext): Promise<{
    success: boolean;
}>;
