import { ExecutorContext } from '@nx/devkit';
import { LintExecutorSchema } from './schema';
export default function runExecutor(options: LintExecutorSchema, context: ExecutorContext, checkstylePath: string): Promise<{
    success: boolean;
}>;
