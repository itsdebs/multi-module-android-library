import { ExecutorContext } from '@nx/devkit';
import { QuarkusBuildImageExecutorSchema } from './schema';
export default function runExecutor(options: QuarkusBuildImageExecutorSchema, context: ExecutorContext): Promise<{
    success: boolean;
}>;
