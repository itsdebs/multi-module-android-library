import { ExecutorContext } from '@nx/devkit';
import { MicronautBuildImageExecutorSchema } from './schema';
export default function runMicronautBuildImageExecutor(options: MicronautBuildImageExecutorSchema, context: ExecutorContext): Promise<{
    success: boolean;
}>;
