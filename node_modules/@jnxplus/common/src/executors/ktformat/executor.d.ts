import { ExecutorContext } from '@nx/devkit';
import { KotlinFormatExecutorSchema } from './schema';
export default function runExecutor(options: KotlinFormatExecutorSchema, context: ExecutorContext, ktlintPath: string): Promise<{
    success: boolean;
}>;
